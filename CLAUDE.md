# dmu-p4-cheat-sheet

FF14「絶妖精乱舞」フェーズ4（P4）の特定ギミックを攻略するためのカンペ（チートシート）ツール。

> このファイルは Claude Code 向けのプロジェクト指示書です。
> ゲーム仕様・用語・ギミックの正確な定義をここに集約し、実装の「正解」の参照元とします。
> バイブコーディング中でも、仕様の判断はこのファイルを根拠にしてください。

---

## 1. プロジェクト概要

- **目的**: P4 の特定ギミックにおける、各プレイヤーの「立ち位置・行動・順番」を即座に確認できるカンペを提供する。
- **想定利用シーン**: TBD<!-- TODO: 攻略中に別画面で見る / 事前学習用 / マクロ補助 など -->
- **対象プラットフォーム**: TBD<!-- TODO: Web（PC/スマホ）/ オーバーレイ など -->
- **対象ユーザー**: 該当ギミックを練習中のプレイヤー（FF14 用語は前提知識としてよい）。

---

## 2. 技術スタック / 構成

- フレームワーク: React 18 + Vite
- 言語: TypeScript
- スタイリング: プレーンCSS（`src/index.css` に CSS 変数でカラートークン管理）
- ホスティング / ビルド: 静的サイト（`vite build`、`base: "./"` で GitHub Pages 等にデプロイ可）。バックエンド無し
- 状態管理: React `useReducer`（外部ライブラリ無し）
- 実装形態: Web アプリ。利用モデルは**デバフ単位のグローバル入力**（仕様変更#1）。8人個別入力は廃止し、グランドクロス／全体攻撃ごとの真偽・早遅の5項目だけを入力 → 処理順マップ（2.3〜2.8）と FF14 マクロを自動出力する（仕様変更#2で混沌の種別入力を廃止し6→5項目）。

### ディレクトリ構成

- `src/logic/` … ドメインロジック（UIから分離した純粋関数）
  - `types.ts` … 型定義（`Truth`/`Timing`/`ActionLabel`）・真偽→行動の解決テーブル（`RESOLVERS`）・表示メタ（`ACTION_META`）
  - `globalInput.ts` … グローバル入力型（`GlobalInput` 5項目 / `isFilled` 型ガード）と、入力→各枠の確定行動への解決（`resolveGlobal`）
  - `macro.ts` … 15行固定テンプレートの FF14 マクロ生成（`buildMacro` / `buildMacroText`）
- `src/state.ts` … `useReducer` の state（`GlobalInput`、デフォルト全 null）/ action
- `src/components/` … UI コンポーネント（GlobalInputPanel / Results / Toggles(`SegToggle`) / Badges(`ActionBadge`)）

### ギミックのデータモデル上の要点（実装の前提）

- **入力は次の5項目のみ。デフォルトは全て未選択（null）**:
  1. GC1(1.2) 真偽
  2. GC1 水圧縮 早/遅（FLも同タイミング＝仕様変更#1）
  3. ほのお（混沌の炎）真偽
  4. GC2(1.4) 真偽
  5. つなみ（混沌の水）真偽
- **混沌は種別で早遅が確定（仕様変更#2）**: 混沌の炎/水は付与順（1.3/1.5）がランダムだが**必ず炎から発動する**。よって早い混沌(2.5)=炎・遅い混沌(2.8)=水で固定。付与順・種別は入力させず、炎の真偽（=早）と水の真偽（=遅）を直接入力する（クリック1回削減、6→5項目）。
- **真偽はキャスト単位**（requires.md 3.1）。GC1由来デバフの真偽=GC1真偽、GC2由来=GC2真偽。
- **水圧縮/FLの早遅→由来GC→真偽が一意に確定**: 仕様変更#1より GC内で水圧縮とFLは同タイミング。2GCで早枠(2.3)・遅枠(2.6)を1枠ずつ埋めるため、GC1の水・FLが早ならGC2は必ず遅、の排他関係。早枠/遅枠の水・FLの由来GCと真偽が入力から一意に導出される。
- **加速度爆弾は早遅とGCを一意に紐付けできない**（仕様変更#1: 1着弾で早遅混在・人数無視）。行動は真偽のみで決まるため「GC1由来→行動 / GC2由来→行動」の2系統で提示し、各自が由来GCで判断（GC1真偽=GC2真偽なら1本化）。
- **呪詛は1回目=早(2.4)・2回目=遅(2.7)で固定**。早=GC1真偽、遅=GC2真偽。
- **2.1 傷・2.2 アラガン/死の超越は GC3(1.6) 由来で入力対象外**。「各自判断」の注記のみ表示し計算しない。
- 全5項目が non-null になった時点で自動出力、以後どれか変更で再出力（仕様変更#1）。出力と同時にマクロを自動でクリップボードへコピーする。
- 仕様に**未記載の具体座標・立ち位置・散開方向は出力しない**。出力は「処理方法」と「処理順」に限定し、未確定点は UI 上で明示する。
- マクロは **15行厳守・1行25文字程度**（固定テンプレートで構造的に溢れない）。

---

## 3. FF14 一般仕様（前提知識）

このツールが依存する FF14 の共通仕様。実装時の座標・方向・ロール定義はここに従う。

### 方角・座標

- フィールドは基本的に円形/正方形のアリーナ。方角は **北(N) を上（12時）** とし、時計回りに N → NE → E → SE → S → SW → W → NW。
- 距離・AoE 範囲の単位はメートル

### ロール / ジョブ

- **タンク (Tank)**: 暗黒騎士、戦士
- **ヒーラー (Healer)**: 占星術師、学者
- **DPS**: ヴァイパー、リーパー、踊り子、召喚士
- 「MT/ST」「H1/H2」「D1〜D4」の標準割り当て表記を用いる。

### 頭割り / 散開 / デバフ

- 頭割り（Stack）、散開（Spread）、ペア（Pair）の標準的な意味。
- デバフ（debuff）の優先度・処理順の一般原則。
- このプロジェクトで扱うデバフ一覧と効果は「4. ギミック仕様」に記述。

---

## 4. FF14 マクロ仕様について

### /party

- /party(/p) 文章
  - 同じパーティメンバー全員に、距離、エリアを問わず文章を送信する。
  - /party(/p)のみを入力した場合は、デフォルトの会話モードをPartyに変更する。

### /hotbar

- /hotbar サブコマンド
  - ホットバーを操作、設定する。PvEエリアでのみ実行可能。
  - サブコマンド
    - set(action) "アクション名" 番号1 番号2
　    - …指定したアクションを指定した番号1のホットバーの指定した番号2のスロットにセットする。番号1に"current"を指定した場合、現在ホットバー1に表示されているセットを示す。番号2を省略した場合、最も小さい番号の空きスロットにセットする。番号1、番号2を省略した場合、最も小さい番号のホットバーの最も小さい空きスロットにセットする。
    - copy クラス・ジョブ名1 番号1 クラス・ジョブ名2 番号2
　    - …クラス・ジョブ1のホットバー番号1の内容を、クラス・ジョブ2のホットバー番号2に上書きコピーする。クラス・ジョブ名に"current"を指定した場合、現在のクラス・ジョブを指定する。クラス・ジョブ名に"share"を指定した場合、共有ホットバーを指定する。
    - remove 番号1 番号2
      - …指定した番号1のホットバーの指定した番号2のスロットにセットされたアクションやマクロなどを解除して、空のスロットにする。番号2を"all"で指定した場合、すべてのスロットを解除する。

---

## 5. 設計方針 / コーディング規約

- **UI は「一瞬で読める」ことを最優先**: 攻略の合間に見るため、情報密度より視認性。
- 表記ゆれを避ける: ロール表記・方角表記は本ファイルの定義に統一する。
- <!-- TODO: 命名規則・コンポーネント分割方針など -->

---

## 6. Claude Code への指示

- ギミック仕様に関わる実装は、必ず「4. ギミック仕様」の記述を根拠にする。仕様が未記載なら推測せず確認を求める。
- FF14 用語（ロール/方角/頭割り等）は「3.」の定義に従う。揺れを見つけたら指摘する。
- 仕様が変わったら、コードだけでなくこの CLAUDE.md も更新する。
